sap:
  clusters:
    - id: qt
      url: http://sdfjshdfg
      accounts:
        # Аккаунт для ....
        - id: acc1
          password: ${ACCOUNT_PASSWORD}
          roles:
            - name: role1
            - name: role2
          companies:
            - id: Company1
              name: Company1
              description: sdfsdf
              type: demo
            - id: Company2
              name: Company2
              description: sdfsdfasdasd
              type: demo
          users:
            - id: user1
              name: user1
              first-name: John
              last-name: Smith
              email: sdf@sdasd
              password: ${USER1_PASSWORD}
            - id: user2
              name: user2
              first-name: Sarah
              last-name: Johnson
              email: sdf222@sdasd
              password: ${USER2_PASSWORD}
        - id: acc2
    - id: et
      url: http://sdjfhskdjf

security:
  providers:
    - id: local
    - id: vault
      url: http://vault.hhh

appium:
  running-profiles:
    - id: local
      appium-server-url: http://localhost
    - id: saucelab
      appium-server-url: http://saucelab.com
    - id: some
      appium-server-url: http://some.com

app:          # field: AppUnderTestConfig
  platforms:  # field: List<AppPlatform>
    - id: ios                       # field: String
      platform-version: 12          # field: String?
      app-name: ios app name        # field: String
      device-name: ios device name  # field: String
      locale: EN                    # field: String?
    - id: android                   # field: String
      platform-version: 13          # field: String?
      app-name: android app name    # field: String
      device-name: android device name  # field: String
      locale: EN                    # field: String?

app2:         # field: App2UnderTestConfig
  platforms:  # field: Map<AppPlatform>
    ios:      # key in Map
#      id: ios      # used as a key
      platform-version: 12          # field: String?
      app-name: ios app name        # field: String
      device-name: ios device name  # field: String
      locale: EN                    # field: String?
    android:  # key in Map
#      id: android  # used as a key
      platform-version: 13          # field: String?
      app-name: android app name    # field: String
      device-name: android device name  # field: String
      locale: EN                    # field: String?

#debug:
#  skip-login: true
#  skip-logout: true
